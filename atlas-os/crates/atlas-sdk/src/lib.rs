// Atlas-SDK: Developer Toolkit for Atlas-OS

pub mod client {
    // Future home of gRPC client wrappers
}

pub mod utils {
    // Re-export common types for easier access
    pub use atlas_registry::EntityType;
}
